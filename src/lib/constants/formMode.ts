export enum FORM_MODE {
  view = "view",
  edit = "edit",
  preview = "preview",
  generate = "generate",
  portalNew = "portalNew",
  portalEdit = "portalEdit",
  portalView = "portalView"
}

export enum FORM_DELIVERY_MODE {
    portal = "portal",
    generate = "generate",
}